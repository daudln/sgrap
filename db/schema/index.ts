import { user, session, verificationToken } from "./uaa";
import { school } from "./school";
import { profile } from "./profile";

const schema = {
  user,
  session,
  verificationToken,
  school,
  profile,
};
export default schema;
